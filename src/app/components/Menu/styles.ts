import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: 105,
    height: 105,
    backgroundColor: "white",
    borderRadius: 8,
    marginLeft: 15
  }
});